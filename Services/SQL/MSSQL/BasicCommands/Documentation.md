# Basic Commands for MSSQL

---

```
SELECT SUSER_NAME();        -- Basically, whoami
SELECT IS_SRVROLEMEMBER('sysadmin');  -- maybe we are sysadmin (1 = yes, 0 = no)
SELECT USER_NAME();         -- Show database user
```

